/*
const client = new MongoClient(uri, { useNewUrlParser: true }); //goood!

client.connect(err => {
  const collection = client.db("test").collection("devices");
  client.close();
});
  //only thing I am curious is about referencing the collection... Step too far. just wanna connect
  // perform actions on the collection object
*/